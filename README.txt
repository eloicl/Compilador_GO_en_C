Compilador de lenguaje Go desarrollado en C.

Compilar:
	Desde la carpeta con los archivos del código fuente y el fichero de entrada
	
	gcc abb.c errores.c tablaSimbolos.c sistemaEntrada.c analizadorSintactico.c analizadorLexico.c main.c -o ej -Wall

Ejecutar:
	En esa misma carpeta llamar al ejecutable generado
	
	./ej
